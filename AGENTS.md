# AGENTS.md - 麻雀ゲーム & AI牌読みコーチングシステム 開発ガイド

このドキュメントは、本プロジェクトを開発・保守するAIエージェントおよび開発者のための総合引き継ぎガイドです。
セッションを跨いで開発を進める際は、まずこのファイルを読み、現在のPhaseとアーキテクチャ方針を把握してください。

---

## 1. プロジェクト概要 & ビジョン

### 🎯 目的
- **ブラウザベースの4人麻雀ゲーム**（1プレイヤー vs 3コンピュータ）
- **AI牌読みコーチング機能**: 対局中の任意の局面で、プレイヤーの視点情報（公開情報のみ）を元に、ローカルCLI（Antigravity CLI `agy` や `claude -p`）を直接呼び出し、ロジカルな牌読み（筋・壁・現物・序盤捨て牌・手出しツモ切り）や打点・何切るアドバイスをチャットで提供する。
- **プレイヤーの上達を促す学習体験**: 単に答えを教えるだけでなく、「なぜこの牌が危険なのか」「なぜこの選択が受け入れ最大なのか」「符と翻数の根拠」を論理的に解説する。

### ⚠️ 最重要ルール：完全な不完全情報の保証
- **AIコーチには、他プレイヤー（CPU）の非公開手牌や山牌の情報を絶対に渡さないこと。**
- AIに渡すデータは必ず `SanitizedPlayerView`（自手牌、全員の捨て牌/河、副露、ドラ表示牌、点数、自風/場風、残り山枚数・巡目）に限定する。
- これにより、チートのない「人間と同じ目線での本物の牌読み」を実現する。

---

## 2. 段階的ロードマップ & 実装ステータス

| Phase | 内容 | ステータス | 詳細ドキュメント |
|---|---|---|---|
| **Phase 0** | 設計、AGENTS.md、環境構築、型定義、プロンプト生成、CLIブリッジ | 進行中 / 基盤完成 | `docs/ARCHITECTURE.md`, `docs/ROADMAP.md` |
| **Phase 1** | 麻雀コアルール（牌山、シャンテン数、受け入れ枚数、和了判定） | 次のステップ | `docs/ROADMAP.md#phase-1` |
| **Phase 2** | 4人対局ゲームループ、簡易CPU、基本盤面UI、局進行 | 予定 | `docs/ROADMAP.md#phase-2` |
| **Phase 3** | 副露（ポン/チー/カン）、リーチ、役・符・点数計算、流局・連荘 | 予定 | `docs/ROADMAP.md#phase-3` |
| **Phase 4** | 牌読みルールベース分析、対局中ワンクリック相談、LLM応答最適化 | 予定 | `docs/AI_COACHING_SPEC.md` |
| **Phase 5** | 麻雀牌演出・アニメーション、牌譜保存、局面検討リプレイモード | 予定 | `docs/ROADMAP.md#phase-5` |

---

## 3. ディレクトリ構成 & 各モジュールの役割

```
game-ai-mahjong/
├── AGENTS.md                  # 本ファイル（エージェント引き継ぎ用総合ガイド）
├── README.md                  # ユーザー向け概要 & 起動ガイド
├── docs/                      # 詳細設計書群
│   ├── ARCHITECTURE.md        # システム構成 & CLIブリッジ設計
│   ├── ROADMAP.md             # 段階的ロードマップ & タスク進捗表
│   ├── DATA_MODELS.md         # 牌・手牌・盤面・コンテキストの型仕様
│   └── AI_COACHING_SPEC.md    # 牌読みプロンプト設計 & CLI連携仕様
├── server/                    # ローカルCLIブリッジサーバー (Node.js/Express)
│   ├── index.ts               # APIサーバーエントリー (/api/coach/chat 等)
│   └── cliRunner.ts           # agy / claude コマンドのプロセス実行モジュール
├── src/
│   ├── core/                  # 麻雀コアルールエンジン（React非依存・Pure TypeScript）
│   │   ├── types/             # 牌・手牌・ゲーム状態の型定義
│   │   ├── constants/         # 牌定数・役定義・点数表定数
│   │   └── utils/             # 牌ソート・表記変換・シャンテン数計算など
│   ├── ai/                    # AIコーチング & 局面テキスト化モジュール
│   │   ├── types/             # SanitizedPlayerView, CoachQuery 型
│   │   └── prompt/            # 局面Markdownプロンプトフォーマッタ
│   ├── components/            # React UIコンポーネント (麻雀卓、手牌、河、チャットパネル)
│   ├── App.tsx                # メインアプリケーション
│   └── main.tsx
└── tests/                     # Vitest 単体テスト群
```

---

## 4. 開発コマンド一覧

```bash
# 依存パッケージインストール
npm install

# フロントエンド & ローカルCLIサーバーの同時起動
npm run dev

# 個別起動
npm run dev:web      # フロントエンド (Vite: http://localhost:5173)
npm run dev:server   # ローカルCLIブリッジサーバー (Express: http://localhost:3001)

# テスト実行
npm test             # Vitest 単発テスト
npm run test:watch   # Vitest ウォッチモード

# ビルド & 型チェック
npm run build
```

---

## 5. コーディング規約 & 設計原則

1. **コアルールとUIの完全分離**:
   - `src/core/` 配下は React や DOM に一切依存しない純粋な TypeScript 関数・クラスとする。すべて単体テスト可能に保つ。
2. **不完全情報のサニタイズ（重要）**:
   - UIやAIに局面データを渡す際は、必ず `sanitizeForPlayer(gameState, playerIndex)` を介し、他家の非公開手牌や山牌を除去した `SanitizedPlayerView` を使用する。
3. **牌の表現とフォーマット**:
   - 牌のID: `1m`〜`9m` (萬子), `1p`〜`9p` (筒子), `1s`〜`9s` (索子), `1z`〜`7z` (字牌: 東南西北白發中)。赤ドラは `0m`, `0p`, `0s` または `isRedDora: true`。
   - LLMに渡すテキスト表現: 日本語表記（例: `[一萬, 二萬, 三萬, 四筒, 赤五筒, 六筒, 七索, 八索, 九索, 東, 東, 白, 白]`）と英数字表記の両方を併記し、LLMが誤読しないよう工夫する。
4. **ローカルCLI実行のフォールバック**:
   - `agy` や `claude` がローカル環境で見つからない・エラーになった場合でも、ルールベースのモック応答や丁寧なエラーメッセージを返してアプリがクラッシュしないようにする。

---

## 6. セッション引き継ぎ時のチェックリスト

新しいセッションを開始したエージェントは、以下の順序で作業を進めてください：
1. `git status` で作業ツリーを確認。
2. `AGENTS.md` と `docs/ROADMAP.md` を確認し、現在どのPhaseに取り組んでいるかを把握。
3. `npm test` を実行して既存テストが全てパスすることを確認。
4. 対象Phaseのタスクを実装し、必ず単体テストを追加。
5. 実装完了後、`docs/ROADMAP.md` のチェックボックスとステータスを更新。
6. タスク管理リポジトリ（`/Users/s-ikari/work/project/task-management`）の `TASK-020.md` を更新してコミット・プッシュ。
